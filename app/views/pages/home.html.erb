<!--
<% if user_signed_in? %>
  <%= link_to "Log Out", destroy_user_session_path, method: :delete %>
<% else %>
  <%= link_to "Log In", new_user_session_path %>
<% end %>
-->

 <div class="banner container-hp">

<!--   <div class="banner-text text-center">
    <h1>Pages#home</h1>
    <p>Find me in app/views/pages/home.html.erb</p>
  </div> -->

  <!-- <div class="banner-text" style="height:75%";> -->
  <div class="banner-text text-center">
    <h1>Networking</h1>
    <p>made easy</p>
  </div>

    <div class="row">
      <div class='col-xs-12 col-sm-10 col-sm-offset-1 col-md-6 col-md-offset-3'>
        <!--
        <form action="" class="form-inline search-bar">
          <div class="form-group">
            <input type="text" class="form-control" placeholder="Where are you">
          </div>

          <div class="form-group">
            <input type="dropdown-toggle" class="form-control" placeholder="Category">
          </div>


          <button class="btn btn-success">Search</button>
        </form>
        -->

        <div class="search-bar">
          <%= simple_form_for @event, html: { class: "form-inline" }, method: :get do |f| %>
           <%= f.input :city, placeholder: "city", label: false %>
            <%= f.input :category, placeholder: "Category", collection: @categories, include_blank: "See all categories", label: false %>
            <%= f.button :submit, 'Search', class: "btn btn-success" %>
          <% end %>
        </div>

      </div>
    </div>

</div>

